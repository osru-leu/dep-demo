apiVersion: github.example.org/v1alpha1
kind: RepoClaim
metadata:
  name: demo-repo
spec:
  name: gitops-crossplane-demo
  description: "A demo repo created via GitOps with Crossplane: syncing..." 
  private: false 
  autoInit: true